# sample-app-influxdb
Sample Demo Application with Influx DB

### Dependencies
- Login component
- Modules: momentjs, underscore, influxdb, hogan
- UIComponents
- Define the influxdb credentials in "influxDBConfig" object under "app/config/config"
- Use "app/simulator/simulate" to trigger device events
